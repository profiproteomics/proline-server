package fr.proline.module.exporter.commons.config

/**
 * Represents the configuration for a field in the customizable export.
 */
case class CustomFieldConfig(id: String,title: String, defaultDisplayed: Boolean = true)
